name: Test

on:
  push:
    paths-ignore:
      - README.md

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          # single source of truth for the toolchain: the go directive in go.mod
          go-version-file: go.mod
      - name: Lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.13.2
      - name: Build
        run: go build ./...
      - name: Test
        run: go test -race -v ./...